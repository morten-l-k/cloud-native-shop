# Front-end tests

## Install test dependencies
To install dependencies for front-end testing, run the following commands from inside the tests directory
```bash
npm install
npx playwright install
```

## Run tests
To run the front-end tests for the project run the following from the tests directory
```bash
npx playwright test
```